# Vault Expressions

::: v-pre
Tolaria no longer evaluates `{{...}}` vault expressions. The previous HTML-block expression runtime and spreadsheet reference helpers have been removed.

Existing Markdown containing these strings remains source text. Raw mode can be used to edit or migrate it; Tolaria will not resolve the expressions, execute scripts, or render an HTML block preview.
:::
